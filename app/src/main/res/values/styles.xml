<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Light and Dark and not using values-night folder to independently change widget theme from system theme -->

    <!-- TextView styles -->
    <style name="PrimaryTextStyle">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/textColorPrimary</item>
        <item name="android:textStyle">bold</item>
        <item name="android:layout_margin">4dp</item>
        <item name="android:ellipsize">end</item>
        <item name="android:maxLines">1</item>
    </style>

    <style name="SecondaryTextStyle" parent="PrimaryTextStyle">
        <item name="android:textSize">12sp</item>
        <item name="android:textColor">@color/textColorSecondary</item>
        <item name="android:textStyle">normal</item>
    </style>

    <style name="SectionHeaderTextStyle" parent="SecondaryTextStyle">
        <item name="android:shadowColor">@color/text_shadow_color</item>
        <item name="android:shadowDx">.0</item>
        <item name="android:shadowDy">2.0</item>
        <item name="android:shadowRadius">8.0</item>
        <item name="android:letterSpacing">.1</item>
        <item name="android:textAllCaps">true</item>
    </style>

    <!-- styles for light backgrounds -->
    <style name="PrimaryTextStyle.Light" parent="PrimaryTextStyle">
        <item name="android:textColor">@color/textColorPrimaryLight</item>
    </style>

    <style name="SecondaryTextStyle.Light" parent="SecondaryTextStyle">
        <item name="android:textColor">@color/textColorSecondaryLight</item>
    </style>

    <style name="SectionHeaderTextStyle.Light" parent="SectionHeaderTextStyle">
        <item name="android:textColor">@color/textColorSecondaryLight</item>
    </style>
</resources>
